name: kriya_test
description: Kriya Test

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^4.0.1
  get: ^4.3.8
  json_annotation: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.4
  flutter_lints: ^1.0.0
  json_serializable: ^6.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/webp/